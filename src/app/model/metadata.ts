import { ElementRef } from "@angular/core";
import { Style } from "../model/enum/style.enum";

export class Metadata{
    headerStyle : string;
    headerFont : string;
    fontSize : string;
    textStyle : Style;
}